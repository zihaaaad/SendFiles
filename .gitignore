node_modules/
build/*
# ...but the Electron app icon is a source asset, not build output.
!build/icon.png
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
release/
dist-electron/
release-desktop/

# Separate project that lives inside this directory; not part of SendFiles.
SendApp/
